<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Покупка</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>

<header>
    <div class="logo">Harley-Davidson</div>
</header>

<h1 style="text-align:center; margin-top:30px;">Каталог мотоциклов</h1>

<div class="cards">
    <% bikes.forEach(bike => { %>
        <div class="card">
            <img src="<%= bike.image %>" alt="<%= bike.name %>">
            <h3><%= bike.name %></h3>
            <p>Цена: <%= bike.price %> $</p>

            <form action="/buy/<%= bike._id %>" method="POST">
                <button type="submit">Купить</button>
            </form>
        </div>
    <% }) %>
</div>

</body>
</html>
